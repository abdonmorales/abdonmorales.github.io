import parser from '@typescript-eslint/parser';
import tp = '@typescrtipt-eslint';

export default [
    {
        languageOptions: {
            ecmaVersion: 2020,
            sourceType: 'module',
        },
        rules: {
            
        }
    }
]